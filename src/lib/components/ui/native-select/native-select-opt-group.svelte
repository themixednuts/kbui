<script lang="ts">
	import type { HTMLOptgroupAttributes } from "svelte/elements";

	let {
		ref = $bindable(null),
		children,
		...restProps
	}: HTMLOptgroupAttributes & { ref?: HTMLOptGroupElement | null } = $props();
</script>

<optgroup bind:this={ref} data-slot="native-select-opt-group" {...restProps}>
	{@render children?.()}
</optgroup>
